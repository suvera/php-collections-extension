// $Id$
// vim:ft=javascript

// Otherwise, use ARG_ENABLE
ARG_ENABLE("collections", "enable collections support", "no");

if (PHP_COLLECTIONS != "no") {
	EXTENSION("collections", "collections.cpp");
}

